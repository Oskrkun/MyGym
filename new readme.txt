text
MiGym/
├── index.html
├── css/
│   └── styles.css
├── js/
│   ├── core.js        ← datos + persistencia + helpers
│   ├── routine.js     ← vista rutina + timer + modal días
│   ├── admin.js       ← panel admin + export/import
│   ├── progress.js    ← perfil + peso + heatmap + gráfica
│   └── apps.js        ← navegación + bootstrap
└── gifs/